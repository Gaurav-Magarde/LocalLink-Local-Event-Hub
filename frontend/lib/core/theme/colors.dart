import 'package:flutter/material.dart';

class EColors {
  // ==========================================
  // YOUR VARIABLES (Mapped to the Dribbble Image)
  // ==========================================

  // The deep, almost-black Navy color used for the main "Login" and "Submit" buttons
  static const Color primary = Color(0xFF1A1D2E);

  // A slightly darker shade for when the button is pressed
  static const Color primaryDark = Color(0xFF0F111A);

  // The soft blue-grey from the clouds/illustrations for secondary elements
  static const Color secondary = Color(0xFFE2E8F0);

  // The bright Sky Blue used in the "Travel Tickets" and "Book Tickets" text
  static const Color accentPrimary = Color(0xFF2CB1E6);

  // Pure white, used for the inside of the TextFields (pill shapes) and Cards
  static const Color backgroundPrimary = Color(0xFFFFFFFF);

  // The soft, cool grey-blue background of the entire screen (Scaffold)
  static const Color background = Color(0xFFF2F6F9);

  // Deep navy for the main headings ("Sign Up to Explore", etc.)
  static const Color textPrimary = Color(0xFF1A1D2E);

  // Soft grey for the hint text ("Enter your email", "Forgot password?") and icons
  static const Color textSecondary = Color(0xFF8A8D9F);

  // Your legacy variables
  static const Color light = Color(0xFFFFFFFF);
  static const Color dark = Color(0xFF000000);

  // ==========================================
  // HELPFUL EXTRAS FOR FORMS
  // ==========================================

  // Very light, almost invisible grey for the un-tapped TextField borders
  static const Color borderSoft = Color(0xFFE6EBF2);

  // Crisp red for form validation errors
  static const Color error = Color(0xFFFF4C4C);
}